name: Keep HF Space Alive
on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: curl -v https://adityarajsahu-llm-serve-optimizer-env.hf.space/health > /dev/null

# Core OpenEnv framework
openenv-core==0.2.2
 
# Web server
fastapi==0.135.2
uvicorn[standard]==0.42.0
websockets==16.0
 
# Inference script
openai==2.29.0
pydantic==2.12.5
 
# Utilities
requests==2.32.5
psutil==7.2.2
pytest==9.0.2
 
# ── GPU mode (optional) ───────────────────────────────────────
# Install these only if you have a GPU and want real vLLM execution.
# On CPU / HF Spaces, these are NOT needed — the simulator uses the
# lookup table instead.
#
# pip install vllm --torch-backend=auto